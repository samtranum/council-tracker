%header.view-header
  .wrapper{role: 'layout'}
    %h1 Edit Councillor

%main.view-content{role: 'layout'}
  .wrapper{role: 'layout'}
    = render 'form'
    = link_to 'Show', [:admin, @councillor]
    |
    = link_to 'Back', admin_councillors_path
