%header.view-header
  .wrapper{role: 'layout'}
    .breadcrumbs
      .breadcrumb= link_to 'Users', admin_users_path
      .breadcrumb New user

.view-content.-alpha
  .wrapper{role: 'layout'}
    = render partial: 'form', locals: {user: @user}
