.enumerable.-correction
  .content
    .title #{ correction.body }

  .detail
    .submitter= correction.name
    - if correction.council
      .council= correction.council.name
    .date= correction.created_at
